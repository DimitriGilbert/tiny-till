Create a client-side image upload component with drag-and-drop support. Implement strict validation logic to check MIME types (PNG, JPEG, WebP) and enforce dimension limits (128x128px). Handle file reading to Base64 strings, implement storage optimization, and generate detailed user-facing error messages for validation failures.
Develop the core import execution logic with intelligent conflict resolution. Implement ID matching to identify existing records and apply the overwrite strategy (merge vs replace). Ensure data integrity during the write process by handling updates atomically. Integrate the execution flow to trigger upon user confirmation from the preview modal.
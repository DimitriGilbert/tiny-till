Develop a manual column count override slider component with a range of 2-8 columns. Implement the logic to override the automatic column calculation when the user interacts with the slider. Build a live preview section that immediately shows grid changes as the slider moves. Add a responsive behavior indicator that displays the current column count, distinguishing between manual overrides and auto-calculated values based on screen width.
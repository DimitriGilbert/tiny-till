Design and build a modal overlay for manual quantity entry featuring a custom large numeric keypad optimized for touch. Implement comprehensive input validation that accepts non-negative integers only, rejects decimals with clear error feedback, and handles 0 input to remove the item. Integrate the overlay with the product card state.
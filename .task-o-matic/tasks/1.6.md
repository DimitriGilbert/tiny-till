Configure TanStack Router with file-based routing for all required routes: root, Tally, Settings, and Catalog. Create route files under appropriate directories, define layout routes, set up route guards, lazy loading, and navigation utilities. Implement root route redirection logic, type-safe route parameter handling, and integrate routing with the application layout. Create basic navigation links.
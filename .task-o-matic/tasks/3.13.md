Create a responsive preview modal UI that displays a summary of the import operation. Implement logic to compare the validated import data against the current local catalog state. Calculate and display the breakdown of products to be added versus updated, providing a detailed list of changes for user review before confirmation.
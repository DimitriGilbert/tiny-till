Define TypeScript interfaces for products with UUID identification, integer price storage (cents), and base64 image fields. Implement state management using TanStack Store or React Context. Create persistence layer with IndexedDB/LocalStorage. Build utility functions for UUID generation and currency formatting (cents ↔ display strings). Establish data integrity validation rules.
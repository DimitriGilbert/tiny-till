Implement the file picker component with drag-and-drop support and file type restrictions. Define comprehensive TypeScript interfaces for the catalog schema and implement a validation engine (e.g., using Zod) to parse incoming JSON. Handle syntax errors, type checking, and required field validation, returning structured error messages or valid data objects.
Implement the main tally page layout using Tailwind CSS and CSS Grid. Build the auto-column calculation algorithm that dynamically adjusts column count based on screen width: 2-3 columns for mobile (320px-768px), 4-6 columns for tablet (768px-1024px), and 6-8 columns for desktop (1024px+). Integrate density settings (normal vs compact) that affect both column count and spacing. Include resize event handling, proper grid gap spacing, and smooth responsive behavior updates in real-time.
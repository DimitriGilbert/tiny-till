Define TypeScript interfaces for products including UUID-based identification, price in integer cents, and base64 image fields. Implement local state management using TanStack Store or React Context. Create a persistence layer using IndexedDB or LocalStorage to store products. Develop utility functions for UUID generation and currency formatting between cents and display strings.
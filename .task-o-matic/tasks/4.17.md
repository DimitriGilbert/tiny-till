Build the live total calculation engine using integer arithmetic (cents-based math) and implement currency formatting with Intl.NumberFormat for proper locale support. Create the sticky footer UI displaying the grand total and item count. Implement the clear cart functionality with a confirmation dialog, ensuring all visual updates reflect the state managed in previous tasks.